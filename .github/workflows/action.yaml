name: test_action

on:
  repository_dispatch:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:


jobs:
  remote:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo hello world
